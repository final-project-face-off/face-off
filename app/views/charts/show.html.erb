<%= bar_chart @teams.group('wins').order('wins Desc').limit(5).count %>

<%= pie_chart @teams.limit(5).group('name').sum('wins') %>

<%= column_chart @teams.limit(10).group('name').sum('wins') %>

<%= scatter_chart @teams.pluck('wins', 'goalsPerGame'), xtitle: "Wins", ytitle: "Goals / Game" %>